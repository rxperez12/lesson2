//question 1
// let numbers = [1, 2, 3, 4];
// numbers.length = 0;
// numbers.splice(0, numbers.length);

// while (numbers.length) {
//   numbers.pop();
// }

// question 2
// + sign doesn't concat two arrays in javascript
// makes them into strings and then adds those

// question 5
// function isColorValid(color) {
//   if (color === "blue" || color === "green") {
//     return true;
//   } else {
//     return false;
//   }
// }

// function isColorValid(color) {
//   return color === "blue" || color === "green";
// }
